# Focused workspace implementation — 6 September 2026

The approved redesign is implemented as the 0.2 local macOS build. This is an implementation checkpoint, not a claim that every future Fleet feature is complete.

## Approved homepage — activated

- Wide-screen refinement: removed the homepage's 1240px maximum width. Content now fills the available width with responsive outer gutters capped at 48px; the attention column grows from 280px to 380px. Existing tablet/mobile breakpoints remain. Preview rebuilt independently of the live app and checked at 700, 1440, 1920 and 2560px without horizontal overflow; temporary browser sizing reset after QA.
- Approved homepage: inline New project/Open folder/Quick chat actions, one featured conversation with a real summary, compact project rows with paths/branches/chat counts/activity dates, up to four other recent threads, and a conditional attention list for unresolved findings, failures and completed coding work. No placeholder statistics; hidden examples, deleted chats, terminals and routine reviewer successes are excluded appropriately. All 73 server and 63 UI tests pass; production build succeeds.
- Activated the production frontend through Fleet's View → Reload menu, without restarting its daemon. Desktop screenshot confirms the fluid homepage with the three visible non-demo projects; all four stored projects, 17 chats and the existing open terminal remain intact. The desktop package was rebuilt separately at `release/onboarding-update/mac-arm64/Fleet.app`; the running bundle was not overwritten. Backend/onboarding activation still awaits a safe daemon restart and was not required for this homepage update.
- Visual-only review at `http://127.0.0.1:55889` uses an in-memory, redacted snapshot of the local workspace. The labelled existing-demo-history toggle demonstrates the populated layout without adding records; normal non-demo projects currently have mostly empty chats. Preview actions only display a notice, and the server rejects mutations. Build and preview harness live under ignored `build/home-review/`; the preview-only banner and demo toggle are not shipped in the app.

## Delivered

- Onboarding reduced to a single permission screen: Standard/YOLO → Start using Fleet → Home. Removed Workflow and Review team steps, progress navigation, model discovery, and their unused styles/state. Model and workspace choices remain in chat controls; team options remain in project setup without onboarding-driven pre-filling. Permission-only saves need no workflow fields and preserve older stored defaults instead of resetting them. All 73 server + 61 UI tests pass, including direct completion, error retry, skip, reopening, absent workflow/team controls, no discovery request, legacy preservation and YOLO consent. Browser preview at port 61590 refreshed and verified; the staged desktop package was rebuilt. Main Fleet and its open terminal were left running unchanged.
- Permission onboarding simplified to two choices: Standard (recommended, selected by default) and explicit opt-in YOLO. Standard maps to workspace-write for fresh coding/Scratch chats even if setup is skipped. Read-only remains available in conversation settings, remembered overrides and reviewer sessions; no existing session or saved preference is migrated. Reopening legacy read-only onboarding proposes Standard but applies nothing until Save. Two equal-width cards replace the three-choice layout; sidebar copy no longer incorrectly promises read-only main chats.
- Standard-default verification: full check passed (71 server + 61 UI tests), then an additional shutdown regression passed with the workspace/onboarding suites. Editing by default exposed an in-flight security-scan shutdown race; shutdown now drains scans before SQLite closes. Browser preview at `http://127.0.0.1:61590` shows the two-card layout with Standard selected; YOLO still blocks Continue without acknowledgement and no console errors were reported. Preview data remains separate, and the live four projects/17 chats/open terminal were not restarted or modified. Updated desktop package remains staged under `release/onboarding-update/mac-arm64/Fleet.app`.
- First-run onboarding: three compact screens for Read only/Sandboxed edits/explicitly opted-in YOLO, installed-model selection, default main-folder/worktree choice, and optional team/Memory suggestions. Local SQLite preferences apply to new independent chats; existing sessions and explicit project/Scratch overrides are preserved. Settings can reopen setup; skipping makes no preference write. Saving never creates agents or approves teams. Actual worker new/resumed turn policies support full access, with separate acknowledgement, managed-task exclusion and exclusive Fleet execution guards. Official Codex approval guidance informed the policy mapping and warning copy.
- Onboarding verification: 70 server and 60 UI tests pass, including CSRF/consent, persistence, inheritance/overrides, unchanged existing chats, offline discovery, failure recovery, keyboard gating and deterministic new/resumed YOLO protocol tests (no real model calls). Browser QA used isolated data `/var/folders/17/k82_snbx6kbf465bpylrzh340000gn/T/fleet-welcome-qa-scsXmn/data`: YOLO continuation disabled without acknowledgement, saved read-only defaults returned Home and survived reload, Memory pre-filled project setup while team approval remained unchecked; no projects or agents created. Desktop update packaged separately at `release/onboarding-update/mac-arm64/Fleet.app`; activation deferred because the live app has an open terminal. No live permission preferences were changed.
- Recoverable chat deletion: hover/focus sidebar trash icon, confirmation, persistent project-scoped Trash and restore. Deleted chats/linked reviews leave normal state and session search; stale session endpoints return 410. Worktrees, source edits, transcript events and drafts are retained. Server rejects active, queued, shell/preview-owned and team/workflow-managed sessions; watchers stop while trashed. Selection clears safely without automatically opening another terminal. All 64 server and 56 UI tests pass, covering confirmation/cancel, duplicate clicks, restore, CSRF, persistence, linked reviews and unchanged worktree files.
- Deletion browser QA: the isolated worktree chat moved to Trash and restored successfully while a separate test terminal continued running; the active terminal's delete action was disabled. No browser console errors. Test chat restored and QA shell closed; no user chats deleted. Desktop package rebuilt and activated after the user confirmed readiness: the remaining idle shell had no child commands, Fleet was restarted gracefully, and the live state API confirmed Trash support with all four projects and 18 sessions preserved.
- Direct terminal refinement: selecting a standalone terminal immediately opens/reattaches and focuses the shell. Removed the intro, headings, path banner and extra Open button; kept a discreet close icon and explicit retry/reopen controls after errors or closure. Ownership and writer locks remain enforced by the backend. All 54 UI tests pass, including single-open behaviour under StrictMode, retry after a failed lease and no automatic reopening after close. Desktop package rebuilt. Supersedes the two-step terminal opening described in the initial sidebar walkthrough below.
- Sidebar + actions: compact menu for an immediately created Git worktree, read-only main-folder chat, or standalone project terminal. No model/shell starts on entry creation. Main chats preserve existing source edits and cannot use Fleet's automatic stage/commit acceptance; terminals retain explicit local-shell confirmation, ownership leases and concurrent-writer protection. Menu dismissal, action targeting, duplicate-click protection and dedicated terminal routing are covered by UI tests; backend tests exercise actual Git worktrees, unchanged staged source files, CSRF, and a real project-folder PTY. All 61 server and 53 UI tests pass; production build succeeds.
- Sidebar browser walkthrough: isolated Sidebar demo fixture verified the unclipped three-action menu, idle main chat, immediately created task branch, standalone terminal, explicit shell opening, real `pwd` output in the correct project, and shell close. No browser console errors. Fixture chats remained at attempt 0; no user-project test entries or model calls were made. macOS package rebuilt and Fleet reopened after checking there were no active user agents, shells or previews.
- Brain file explorer: shared searchable, alphabetically sorted Markdown file list in both graph and Notes modes, selected-file highlighting, new-note action, vault count and hide/show control. Search is independent of graph filters; graph and file selections stay synchronized. Narrow-window overlay closes after successful selection; unsaved-edit protection still applies. All 51 UI tests pass, including independent file search, graph/file selection, collapse state, narrow selection and automatically added notes. Browser checked the 13-note fixture, note reader, search and collapse. No backend or user-vault content changes.
- Graph motion: independent, damped force simulation with bounded frame scheduling, initial settling, node pinning during drag, connected-node response, drag-release settling and continuity when notes/force settings change. Display has Animate/Pause controls (layout replay, not historical time-lapse). Hidden-tab pause/resume, runtime reduced-motion changes and unmount cleanup are covered. All 50 UI tests pass; production build succeeds. Live browser checks confirmed settling reaches idle, replay/pause toggles correctly, and a 120px/-60px drag moves the node and its neighbours without opening the reader. No backend or user-vault changes.
- Live Obsidian reference pass: installed Obsidian 1.13.7, opened a separate copy of the 13-note QA vault at `/tmp/fleet-obsidian-reference.Qx5XHJ`, and inspected its actual graph and Display controls in normal and enlarged windows. Corrected Fleet's oversized startup scale, side labels, forced central Home, persistent purple selection, and settings-panel geometry. Labels now sit underneath; the 230px panel opens with collapsed sections; sliders have value rows and pill handles. A viewport-sized SVG preserves pixel scale when resizing/opening the reader. All 48 UI tests pass, including compact startup and ResizeObserver cleanup/scale regressions; production build succeeds. This is Fleet's independent renderer, not copied Obsidian code or an identical physics engine. User vault contents/backend are unchanged. Obsidian's new vault registry contains only the isolated reference vault.
- Project Brain graph: a direct Brain shortcut, per-project linked-note graph with pan/zoom, draggable nodes, fit, search, note-type filtering and one-hop local graph. The adjacent Markdown reader/editor retains wiki navigation, backlinks, provenance, pin/exclude and proposal approval. Generated inventory/receipts use the existing automatic writing pipeline; the open view reloads vault changes every eight seconds without model calls. Note edits have local draft recovery and duplicate-name protection. Graph rendering is bounded to 250 notes; Notes view retains the complete vault.
- Home is the startup screen: a dedicated start column, searchable project cards with real paths/branches and stable identity colours, duplicate-name disambiguation using folder names, and up to three meaningful recent conversations. Empty drafts and legacy team placeholders remain accessible in their workspace; reviewer activity does not reorder cards. New/Open/Scratch actions retain their existing flows. Project workspaces retain a Home button; explicit session links bypass the launcher without starting execution.
- Conversation-first workspace with permanent horizontal **Home · All projects · individual project tabs**. The homepage is retained; project navigation no longer uses a dropdown. Tabs use folder names to distinguish duplicates and scroll horizontally when needed. All projects expands the sidebar, and browsing its sessions retains the all-project scope. Choosing a project focuses its sidebar again. Sidebar filters reset on scope changes while saved conversations and unsent drafts remain intact. Project tools remain available under an ellipsis menu. New/open-project actions live on Home. No separate rail, startup form, guided-build choice or permanent tool tabs in everyday navigation.
- Line Command-inspired one-click conversations, independent Scratch spaces, saved permissions/model defaults and persistent drafts. Tools open beside the conversation, changed-file counts open the diff, and routine reviewer status is hidden while actionable findings/failures remain visible. This is Fleet's own implementation, not imported vendor/dashboard code.
- Frontend split into session, Brain, security, workflows, activity, dialogs and shared UI modules.
- Persistent session selection and drafts; cross-workspace search; committed file previews; event pagination.
- Electron shell and macOS ARM64 packaging, with a reproducible Fleet icon.
- Shared desktop/CLI client, contract types, capability discovery, idempotent mutation handling and ordered SSE replay.
- Detached Codex app-server execution owners, persistent journals, recovery cursors, explicit interruption and resumption.
- Real PTY worktree shell with exclusive ownership, resizing, output retention while the daemon runs, and validation invalidation.
- Ranked, bounded project context, source provenance, stale/proposal exclusion, pin/exclude controls and proposal approval.
- Debounced worktree security scans, coverage reporting, fixed/reopened findings and snapshot-bound acceptance.
- Sandboxed validation by default, with explicit failure when unavailable.
- Approved workflow templates, task dependencies, scope conflict scheduling, retry/task/time limits, automatic checks and human final acceptance.
- Backed-up versioned migration that refuses to interrupt legacy active work.
- Real-repository onboarding with Git inspection, opt-in detected check commands and desktop folder selection. Example creation removed; existing example history hidden by default and recoverable through Settings.
- Live Codex model/authentication discovery, daemon-owned workflow templates and limits, configurable scheduler concurrency, and prompt-content search results.
- Opt-in persistent project teams: Developer, Security, Verification and optional Memory; read-only assessments, snapshot-bound structured reports, bounded review rounds, pause/renew controls and explicit findings-to-fix dispatch.
- New project folders and opt-in local Git initialisation for existing folders, including repositories without commits. Empty checkpoint excludes existing/staged files; empty teams wait for the first task without model calls or review-budget use.

## Verified

- Brain follow-up: all 59 backend + 44 UI tests pass. Graph tests cover real-link extraction, reciprocal-link deduplication, aliases/headings, deterministic finite layout, bounded rendering, keyboard selection, type/local filtering, zoom, unsaved-edit protection, duplicate-name protection and interval refresh cleanup. Existing backend tests cover automatic inventory refresh, receipts, human-note preservation, project isolation, provenance and unapproved/stale-context exclusion.
- Brain browser QA used `/tmp/fleet-home-qa.bAY7Ar/data`: a 12-note/17-link fixture rendered, node selection opened the right note and backlinks, Local graph reduced Security to its four connected notes, and a note written outside the view appeared automatically as the thirteenth node (19 links). That note was edited and saved from the inspector. The fixture session remained at attempt 0; user projects were not seeded with test notes. Narrow-window layout, large-vault usability and prolonged polling/dragging soak testing remain unverified.
- Tab-controls follow-up: 39 UI tests pass. Closing inactive tabs retains the current conversation; closing the active tab returns Home. Closed tabs persist across reloads, and plus reopens the saved conversation/draft. Plus also opens the correct New project/Open folder dialogs without submitting them. These actions send no mutation requests; Home and All projects are not closable. Production frontend build passed.
- Horizontal-navigation follow-up: 38 UI tests pass, including always-visible Home/project buttons, active-scope indication, Home → All projects, project isolation, and preserved drafts without POST requests. Browser checked Home → etsy mcp → All projects → Home against actual user projects. Production build passed; no backend changes. Overflow scrolling is implemented, but a many-project narrow-window visual check was not performed.
- Dropdown-scope follow-up: 38 UI tests pass, including revealing a hidden sidebar through All projects, retaining the current draft, browsing another project's conversation without collapsing scope, then selecting an individual project and returning to the original draft without POST requests. Browser checked with actual user projects; hidden examples remain excluded. Production build passed; no backend changes.
- Project-isolation follow-up: 38 UI tests pass, including overview → project One → overview → project Two, sidebar isolation/filter reset, and preserved unsent drafts without POST requests. Browser-checked the actual etsy mcp workspace: only its two conversations appear, with the current-project marker and View all projects return. Production build and unsigned macOS repackaging passed; no backend changes in this follow-up.
- Idea-first project setup, explicit first-task startup and reviewed text-file working copies with exclusions, stale-approval checks and original-folder preservation.
- Local preview process lifecycle, occupied-port rejection and worktree ownership; UI consent and unsent server-output handoff. Preview execution is explicitly unsandboxed, not a Codex security boundary.

- Backend/desktop and UI interaction suite: 59 + 37 tests at this checkpoint, including Home startup/return/draft preservation, duplicate-name disambiguation, stable card identity during filtering, search clearing, empty states, bounded recent conversations, background-activity exclusion, setup-mode selection, explicit session links, idle scratch isolation, one-click creation, permission restrictions and conversation/tool coexistence.
- Revised Home browser check: inspected the card layout with actual user projects and the isolated Home fixture. Verified search/no-results/clear, conversation resume, unsent-draft preservation and both setup modes without submitting or starting work. Fixture remained at three projects, one session, attempt 0; user workspace remained at four projects (one hidden example) and fourteen sessions. Production build and unsigned macOS packaging passed. Narrow-width breakpoints are implemented but were not visually checked in this pass.
- Home browser walkthrough: real fixture project names/paths/activity rendered; Continue opened the existing conversation without execution; an unsent instruction survived Home/Continue; New project created an approved empty local repository and entered its workspace; a fresh load returned Home. Test data: `/tmp/fleet-home-qa.bAY7Ar/data`. The fixture session remained at attempt 0 throughout; no model calls or user-workspace test records were created.
- Simplification browser walkthrough: one click opened a focused, idle conversation with no startup form; permission changes were explicitly applied without starting it; the next fixture turn created a real worktree file; the changed-file shortcut opened its diff and Tools opened its contents beside the conversation; a second new conversation inherited the remembered editing permission and stayed at attempt 0. Isolated data: `/tmp/fleet-simple-qa.6SjHni/data`. This walkthrough used the deterministic fixture, not a model call.
- Real project-team integration: six model turns, three distinct Codex threads, all resumed for the second phase; structured Security/Verification reports and sandbox checks passed, source unchanged. Temporary evidence: `/var/folders/17/k82_snbx6kbf465bpylrzh340000gn/T/fleet-live-team-Kdvw0U/verification.json`.
- One real model task completed an isolated code repair. Fleet recorded the edit and usage; sandbox validation passed and the original source stayed unchanged. The verification harness initially checked before asynchronous validation completed; corrected it to wait, then successfully reran validation against the same model output without another model call. Reproducible opt-in harness: `node tests/live-session.mjs --run`.
- Real installed Codex 0.153.2 command sandbox: allowed worktree write and denied sibling-directory write; no LLM invoked.
- Five repeatable fixture demonstrations, with measured results in `.fleet/demo-verification.json`.
- Production frontend build and unsigned macOS application packaging.
- Packaged cold startup starts an independent daemon; desktop entry loading has a readiness-deadlock regression test.
- Live browser inspection of the focused workspace, combined review and real terminal.
- September 6 browser walkthrough: blank scratch session remained at attempt 0 with no Codex thread; first instruction completed using the deterministic no-model fixture; current worktree file opened beside the conversation; folder-only working-copy import kept `.env` excluded; project and Scratch groups coexisted; an actual HTTP preview rendered beside the conversation and stopped on request; selection and tool survived reload. Isolated QA data: `/tmp/fleet-workspace-qa.iUMWLL/data`. No model calls or test records were added to the user's workspace.

## Deliberately gated / remaining acceptance work

- **Native Codex TUI takeover:** disabled in capability discovery. The working Shell tool is a worktree shell. Seamless attachment to the same live Codex session needs its own version-specific compatibility and ownership tests.
- **Distribution:** the local macOS build is unsigned/not notarized and requires an existing Node 24+ and Codex installation. Signing/notarization needs distribution credentials. Windows/Linux are out of this release.
- **Memory inference:** optional Memory now proposes semantic notes from initial assessment and accepted work. These are model suggestions, not validated decisions; unapproved proposals never enter task context. The live six-turn smoke test covers Developer/Security/Verification; Memory provenance/exclusion is covered by fixtures, not a live quality benchmark.
- **Evaluation:** fixture results demonstrate system behaviour, not real-world model quality or reduced engineering effort. A representative live-model benchmark and usability testing remain.
- **Execution limits:** live Codex workers can survive a daemon restart; interactive shells and in-flight validation do not. A machine reboot interrupts execution and requires explicit resumption.
- **Dependency reconciliation:** verified file handoffs are conservative; symbolic-link changes and complicated multi-parent conflicts need manual review, not automatic merging.
- **Recovery audit:** tests exercise daemon detachment/reconnection and execution-process-group loss. Arbitrary power loss, disk-full journal writes, independently killed descendant processes and long-running soak tests still need a broader release audit.

No cloud sync, separate account system, scheduled unattended workflows, automatic source merges/pushes, recursive delegation or cross-vendor agent support were added.
